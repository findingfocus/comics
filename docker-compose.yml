services:
  comics:
    build:
      context: .
      dockerfile: Dockerfile
    image: findingfocusdev/comics:latest
    container_name: comics
    ports:
      - "3002:3000"
    restart: unless-stopped